// Inicializar AOS (Animate On Scroll)
AOS.init({
  duration: 1000,      // Duración de la animación en ms
  easing: 'ease-in-out',
  once: true,          // Si quieres que se animen solo una vez
  mirror: false        // Si quieres que se repitan al hacer scroll hacia arriba
});

// Si necesitas más funcionalidades de JS, las puedes agregar aquí
